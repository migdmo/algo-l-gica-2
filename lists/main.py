lista = [2, 4.5, 7, 8]


soma = 0

for n in lista:
    soma += n
print(f"a soma é {soma}")

for i in range(len(lista)):
    print(lista[i])

# palavra = "aba"
# palavra_aux = list(palavra)

# print(palavra_aux)
